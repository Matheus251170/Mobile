import 'package:agenda_contatos/pages/home.dart';
import 'package:flutter/material.dart';
import 'package:agenda_contatos/pages/home.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Home(

    );
  }
}